// import { createFeatureSelector, createSelector } from "@ngrx/store";
// import { Message } from "./message.reducer";


// export const selectFeature = createFeatureSelector<Message>("message");

// export const messageFailure = createSelector(
//   selectFeature,
//   (state: Message) => state.errorMessage
// );

// export const messageSuccess = createSelector(
//     selectFeature,
//     (state: Message) => state.successMessage
// )